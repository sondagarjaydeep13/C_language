

var i , min , max ;

var a = [1,2,3,4,5]

    max = a[0]

    for(i = 0 ; i < a.length ; i++)
     {
    if(a[0] > max)
    {
        max = a[i]
   
    }}
    
    
    min = a[0]
    
    for(i = 0 ; i < a.length ; i++)
    {
        if (a[0] < min){
            
            min = a[i]
            
        }}
        
        
        console.log(min);
        console.log(max);

